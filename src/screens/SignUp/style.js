import {StyleSheet} from 'react-native';

export const style = StyleSheet.create({
  main_view: {
    flex: 1,
    backgroundColor: '#fff',
    rowGap: 20,
    paddingTop: 20,
    paddingHorizontal: 20,
  },
});
