import {StyleSheet} from 'react-native';

export const style = StyleSheet.create({
  main_view: {
    flex: 1,
    backgroundColor: '#fff',
    padding: 10,
    alignItems: 'center',
    justifyContent: 'center',
    rowGap: 20,
  },
});
