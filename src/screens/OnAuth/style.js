import {StyleSheet} from 'react-native';

export const style = StyleSheet.create({
  main_view: {
    flex: 1,
    backgroundColor: '#fff',
    padding: 20,
    alignItems: 'center',
    justifyContent: 'center',
    rowGap: 20,
  },
});
